#include "elf.h"

uint32_t load_elf(const char *path)
{
    DO_NOTHING_WITH(path);
    return -1;
}

int exec(const char *path, char **args)
{
    DO_NOTHING_WITH(path);
    DO_NOTHING_WITH(args);
    return -1;
}
